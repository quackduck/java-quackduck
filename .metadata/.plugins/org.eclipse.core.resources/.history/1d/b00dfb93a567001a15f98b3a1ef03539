
public class FindDuplicateInt {
	
	public int[] thing = {0, 1, 2, 3, 3};

	public static void main(String[] args) {
		new FindDuplicateInt().find(thing);
	}
	
	public int find(int[] arr) {
		
		int result = 0;
		int expectedSum = 0;
		int actualSum = 0;
		for (int i = 0; i < (arr.length - 1); i++) {
			expectedSum += i;
		}
		
		for (int i = 0; i < arr.length - 1; i++) {
			actualSum += arr[i];
		}
		
		return arr[0];
	}

}
